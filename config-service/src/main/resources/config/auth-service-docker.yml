# -----------------------------------------------------------
# Auth Service — Docker config (served by Config Service)
# Eureka URL: "discovery-service" container name
# -----------------------------------------------------------
server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
